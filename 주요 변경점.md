## 요구사항 명세서 변경점 요약

1. 공통 에러 응답 형식 : 간단히 확정한다. code, message만 사용하고 details는 제외하거나 선택값으로 둔다.
2. 파일 업로드 정책 : 최소 구현, 실제 파일은 로컬 /uploads/posts/{post_id}/... 같은 경로에 저장하고 DB에는 file_url만 저장한다. 수정 시 파일이 새로 오면 기존 파일 목록을 전체 교체, 안 오면 유지한다
3. 관리자 권한 부여 정책, 신고 처리 결과 저장 정책 : users.role 기본값은 USER
ADMIN 권한은 회원가입/API로 부여하지 않고 DB seed 또는 운영자 DB 변경으로만 부여, 신고 처리는 report.status, processed_by, processed_at으로 최소 이력만 저장, 신고 처리 API는 관리자만 호출 가능, 신고 처리 자체가 게시글/댓글 삭제까지 자동 수행하지는 않고, 신고 상태만 변경
4. 그룹 링크 UX : 단순 코드 입력 방식, 그룹 생성 시 group_link를 생성해서 화면에 보여주고, 가입자는 그 코드를 입력해서 가입한다. 복잡한 초대 링크/공유 기능은 제외한다.



## 데이터 스키마 변경점 요약

1. post의 is_reported 제거
2. report 테이블 생성



## 추가 사항

+ 작업 시 에이전트가 백엔드 구현에 사용할 정규화된 명세 번들을 생성(normalized 폴더 내 문서들)