# 구현 전 결정 사항 추천

기준: 학부 데이터베이스 중소형 프로젝트

이 프로젝트는 실서비스급 확장성보다 스키마 단순성, 구현 가능성, 발표 시 설명 가능성, 테스트 용이성을 우선하는 것이 적절하다. 따라서 남은 미결정 항목은 과하게 확장하지 않고 작게 확정하는 방향을 추천한다.

## 추천 요약

| 항목 | 추천 처리 |
|---|---|
| 공통 에러 응답 형식 | 간단히 확정한다. `code`, `message`만 사용하고 `details`는 제외하거나 선택값으로 둔다. |
| 일정 `type` 값 | 정수 enum으로 확정한다. 예: `1=수업`, `2=과제`, `3=시험`, `4=스터디`, `5=기타`. DB에는 `CHECK(type IN (1,2,3,4,5))` 정도가 적당하다. |
| 비밀번호 변경 시 현재 비밀번호 | 필수로 받는 것을 추천한다. 아이디/비밀번호 찾기를 제거했기 때문에, 비밀번호 변경은 로그인 상태에서 `current_password` 확인 후 바꾸는 것이 자연스럽다. |
| 파일 업로드 정책 | 최소 구현을 추천한다. 실제 파일은 로컬 `/uploads/posts/{post_id}/...` 같은 경로에 저장하고 DB에는 `file_url`만 저장한다. 수정 시 파일이 새로 오면 기존 파일 목록을 전체 교체, 안 오면 유지한다. |
| `users.role` | 삭제를 추천한다. 관리자/권한 요구사항이 없으므로 남겨두면 설명 부담이 생긴다. |
| `users.status = INACTIVE` | 삭제를 추천한다. 회원 상태는 `ACTIVE`, `DELETED`만으로 충분하며 그룹의 `INACTIVE`와 혼동될 수 있다. |
| `post.is_reported` | 삭제를 추천한다. 신고 기능 요구사항이 없으므로 제거하는 편이 낫다. |
| 개인 일정 상세/수정 UI | 모달을 추천한다. 백엔드 영향이 거의 없고 캘린더 화면 안에서 추가/수정/삭제를 처리하는 것이 구현과 설명이 쉽다. |
| 그룹 링크 UX | 단순 코드 입력 방식을 추천한다. 그룹 생성 시 `group_link`를 생성해서 화면에 보여주고, 가입자는 그 코드를 입력해서 가입한다. 복잡한 초대 링크/공유 기능은 제외한다. |

## 판단

Review 1번과 3번의 남은 항목은 대부분 작게 확정해서 PASS로 넘길 수 있는 상태다.

특히 중요한 것은 미사용 필드 정리다. `users.role`, `users.status = INACTIVE`, `post.is_reported`는 학부 프로젝트에서는 "나중에 쓸 수도 있음"보다 "왜 있는데 안 쓰나요?"라는 질문을 부를 가능성이 크다. 따라서 제거하는 쪽이 더 적절하다.

위 결정을 source 문서와 review 문서에 반영하면 구현 가능 여부는 `CONDITIONAL_PASS`에서 `PASS`로 올릴 수 있다.

## 추가 사항

요구사항 명세서에서 비밀번호/아이디 찾기 관련 글을 모두 제거하였습니다.
logical-schema 문서에서 deletion_due_date 속성을 모두 제거하였습니다.